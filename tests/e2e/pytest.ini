[pytest]
testpaths = tests/e2e
addopts = --strict-markers --verbose -s
markers =
    smoke: Quick smoke tests to verify basic functionality
    slow: Tests that take longer to run
    integration: Full integration tests
