[pytest]
testpaths = tests/e2e
addopts = --strict-markers --verbose -s --ignore fixtures/
markers =
    smoke: Quick smoke tests to verify basic functionality
    library: Tests that use the slurm-executor library
